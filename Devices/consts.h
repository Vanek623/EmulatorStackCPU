#pragma once

#include <QtGlobal>

const quint8 RAM_COM_CAPACITY = 128;
const quint8 RAM_DATA_CAPACITY = 32;
const quint8 RAM_CAPACITY = RAM_COM_CAPACITY + RAM_DATA_CAPACITY;
const quint8 RAM_DATA_BEGIN = RAM_COM_CAPACITY;

const quint8 STACK_CAPACITY = 16;
